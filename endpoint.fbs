namespace endpoints;

enum Action : byte { New, Index, Read }

table Endpoint {
  id:string;
  organizationID:string;
  URL:string;
  action:Action;
  error:string;
  schema:string;
}

root_type Endpoint;
